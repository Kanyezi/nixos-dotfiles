;;禁用工具栏
(tool-bar-mode -1)

;;禁用菜单栏
(menu-bar-mode -1)

;;禁用滚动条
(scroll-bar-mode -1)

;;禁用启动界面
(setq inhibit-startup-screen t)

;;禁用底部横栏
(setq-default mode-line-format nil)

;;禁用左右留白（fringe）
(set-fringe-mode 0)
