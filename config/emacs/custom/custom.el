;; ============================================================================
;; 自定义配置文件
;; ============================================================================

;; 此文件用于存放用户自定义的配置
;; 这些配置不会被版本控制系统覆盖

;; 示例：自定义变量
;; (setq custom-file "~/.config/emacs/custom/custom.el")

;; 示例：自定义主题
;; (load-theme 'modus-operandi t)

;; 示例：自定义快捷键
;; (global-set-key (kbd "C-c C-a") 'my-custom-function)

;; 示例：自定义函数
;; (defun my-custom-function ()
;;   "My custom function."
;;   (interactive)
;;   (message "Hello, World!"))